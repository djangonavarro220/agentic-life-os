{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Agentic Life OS config",
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "enabled": {
      "type": "boolean"
    },
    "runtime": {
      "type": "string"
    },
    "skills": {
      "type": "object",
      "additionalProperties": true
    },
    "semantic_setup": {
      "type": "object",
      "description": "Global semantic install status and pointers to the skill data file that owns each setup answer. Domain answers and source decisions live in per-skill data.json files.",
      "additionalProperties": true
    }
  }
}
